VITE_API_BASE_URL=
# Must match a value in API_CLIENT_KEYS on the API when key enforcement is enabled.
# Exposed as window.AFTERCARE_CONFIG.apiKey for aftercare-text-widget / badge poll embeds.
VITE_API_KEY=
